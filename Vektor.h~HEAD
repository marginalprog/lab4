#pragma once
#ifndef VEKTOR_H 
#define VEKTOR_H

#include <cassert>
#include <iostream>
#include <math.h>
#include <iostream>

class Vektor {
private:
    int m_length;
    double* m_data;

    Vektor();

    Vektor(int length);

    // добавление элемента в конец
    void push_back(double value);

    // вывод первого элемента 
    void front();

<<<<<<< HEAD:Vektor.h
    double& operator[](int index);

    ~Vektor();
=======
    // сортировка
    void swap();

    // удаление элемента в конце Vector
    void pop_back();

    // очищение Vector
    void clear();

    // перезагрузка []
    double& operator[](int index);

    // вывод Vector
    friend std::ostream& operator<< (std::ostream& out, Vector& object);

    ~Vector();
>>>>>>> d6b29c7... Р’РёС‚Р°Р»СЏ РІСЃРµ РїРѕРїСЂР°РІРёР»:Vector.h
};

#endif VEKTOR_H